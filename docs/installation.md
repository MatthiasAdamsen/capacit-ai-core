# Installation

1. Clone the repository:
   `ash
   git clone https://github.com/MatthiasAdamsen/capacit-ai-core.git
   ``n2. Install dependencies:
   `ash
   pip install -r requirements.txt
   ``n3. Set up your environment variables in a .env file.